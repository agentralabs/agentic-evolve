# Quickstart

Install AgenticEvolve with one command:

```bash
curl -fsSL https://agentralabs.tech/install/evolve | bash
```

For profile-specific installs, see [INSTALL.md](../INSTALL.md).

For full quickstart guide, see [docs/public/quickstart.md](public/quickstart.md).
